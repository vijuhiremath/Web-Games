General:
    [N] HTML is simple

Registration Page:
    [N] User feedback on invalid input
        -error messages
        -re-fill form fields
    [N] Use of checkboxes, including responds to updates

Profile Page:
    [N] User feedback on valid input
        -informative messages
    [N] Prefill user information
    [N] User feedback on invalid input
        -error messages
        -re-fill all form fields
    [N] HTML validation on a few >2 text fields

Game stats:
    [N] Display a leaderboard of top X players

The peg jumping puzzle:
    [N] Solvable
        -including user feedback once the puzzle is solved
    [N] Handles edge cases and invalid input
        -error messages
    [N] Simple, yet effective model code

Mastermind:
        [N] Simple, yet effective model code

Style/Usability:
    [N] Simple user interface, easy to use, conforms to web standards
    [N] Great looking user interface, appropriate use of HTML elements

Navigation:
    [N] Navigation appears on all logged in pages

DataBase:
    [N] Foreign keys
    [N] SQL is all used in Model
    [N] Good SQL: Use of powerful SQL queries: select from where group by having etc.

PHP:
    [N] Great code
        -great use of php
        -great naming
        -effective use of PHP tools
        -well-documented code
        -always validate user input

Controller:
    [N] Back button does not cause backend resubmissions (see page tokens)
    [N] Reload button does not cause backend resubmissions (see page tokens)
    [N] Great code
        -great use of php
        -great naming
        -effective use of PHP tools
        -well-documented code
        -always validate user input

Others: 2/3
    [N] No Errors present (With sessions or DB Calls)